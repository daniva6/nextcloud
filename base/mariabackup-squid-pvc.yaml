apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariabackup-squid
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: cephfs-hdd-r2-squid
  volumeMode: Filesystem

