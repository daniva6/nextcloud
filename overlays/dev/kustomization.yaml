apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

patches:
  - target:
      kind: Cluster
      name: nextcloud-pg
    patch: |-
      - op: replace
        path: /spec/instances
        value: 1
      - op: remove
        path: /spec/backup         